
# 📊 Приклад побудови діаграм таблиць (ERD)

## 🎯 Мета роботи

Навчитись:

* читати структуру таблиць
* розуміти звʼязки між ними
* будувати **ERD (Entity Relationship Diagram)**
* пояснювати, **чому звʼязок саме такий**

> ❗ Без SQL
> ❗ Без створення таблиць
> ❗ Тільки аналіз і мислення

---

## 🧠 Що таке ERD (коротке нагадування)

**ERD (Entity Relationship Diagram)** — це:

* візуальна схема бази даних
* показує:

  * таблиці
  * поля
  * звʼязки між таблицями

📌 ERD відповідає на питання:

> «Як дані повʼязані між собою?»

---

## 🛠️ Інструмент: draw.io (diagrams.net)

### Чому draw.io?

* безкоштовний
* працює в браузері
* можна зберігати `.drawio` або `.png`
* ідеально для GitHub

### Як відкрити

1. Перейти на 👉 [https://app.diagrams.net](https://app.diagrams.net)
2. Обрати:

   * Device або Google Drive
3. Створити **Blank Diagram**

---

## 🧱 Приклад предметної області: Інтернет-магазин

Ми працюємо з простою OLTP-моделлю.

### Основні таблиці:

* Users
* Orders
* OrderItems
* Products

---

## 1️⃣ Створення таблиць (entities)

У draw.io:

* обираємо **Rectangle**
* кожен прямокутник = **таблиця**

### Таблиця `Users`

```
Users
----------------
id (PK)
email
name
created_at
```

### Таблиця `Orders`

```
Orders
----------------
id (PK)
user_id (FK)
order_date
status
total_amount
```

### Таблиця `Products`

```
Products
----------------
id (PK)
name
price
category
```

### Таблиця `OrderItems`

```
OrderItems
----------------
id (PK)
order_id (FK)
product_id (FK)
quantity
price
```

📌 **PK (Primary Key)** — унікальний ідентифікатор
📌 **FK (Foreign Key)** — посилання на іншу таблицю

---

## 2️⃣ Побудова звʼязків між таблицями

### 🔗 Users → Orders

* Один користувач
* може мати **багато замовлень**

Тип звʼязку:

> **One-to-Many (1:N)**

У draw.io:

* стрілка від `Users.id`
* до `Orders.user_id`

Пояснення:

> Кожне замовлення належить одному користувачу

---

### 🔗 Orders → OrderItems

* Одне замовлення
* містить багато позицій

Тип:

> **One-to-Many**

---

### 🔗 Products → OrderItems

* Один продукт
* може бути в багатьох замовленнях

Тип:

> **One-to-Many**

---

## 3️⃣ Чому НЕ Orders ↔ Products напряму?

❓ Чому між ними є `OrderItems`?

Бо:

* один товар може бути в багатьох замовленнях
* одне замовлення має багато товарів

👉 Це **Many-to-Many**, яке:

* в реляційних БД
* **завжди розбивається через проміжну таблицю**

📌 `OrderItems` — класичний приклад junction table

---

## 4️⃣ Фінальна ERD (логіка)

```text
Users
  |
  | 1:N
  |
Orders
  |
  | 1:N
  |
OrderItems
  |
  | N:1
  |
Products
```

---

## 🧠 Як Data Engineer має це пояснювати

Студент повинен вміти сказати:

* де транзакційні таблиці
* де довідники
* де ключові звʼязки
* чому структура саме така

---

## ❓ Контрольні питання

1. Чому `Orders` має `user_id`?
2. Навіщо потрібна таблиця `OrderItems`?
3. Який тип звʼязку між Users і Orders?
4. Чи є тут Many-to-Many?

---

## 📝 Завдання (практика)

1. Побудувати ERD у draw.io
2. Зберегти файл:

   * `erd_shop.drawio`
   * або `erd_shop.png`
3. Додати короткий текст:

   * опис кожного звʼязку (1–2 речення)

---

## ✅ Критерії виконання

* всі таблиці присутні
* звʼязки правильні
* PK / FK підписані
* логіка пояснена словами

---

## 💡 Педагогічна ремарка

Це **не про малювання**,
це про:

* читання систем
* архітектурне мислення
* підготовку до OLAP-моделей

---

Якщо хочеш, наступним кроком можу:

* 🔄 **перетворити цю ERD у Star Schema**
* 📦 зробити **версію під AdventureWorks**
* 🏠 або оформити **лабораторну + чеклист для студента**

Скажи, який наступний файл робимо 📁🚀
