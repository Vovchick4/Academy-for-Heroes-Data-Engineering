Структура:

```
users
products
orders
order_items
payments
```

Так роблять **реальні e-commerce системи**.

---

# 1. Створення бази даних

```sql
CREATE DATABASE sql_practice;

USE sql_practice;
```

---

# 2. Створення таблиць

## users

```sql
CREATE TABLE users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100),
    email VARCHAR(100),
    city VARCHAR(100),
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

---

## products

```sql
CREATE TABLE products (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100),
    category VARCHAR(100),
    price DECIMAL(10,2)
);
```

---

## orders

```sql
CREATE TABLE orders (
    id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT,
    order_date DATETIME DEFAULT CURRENT_TIMESTAMP,

    FOREIGN KEY (user_id) REFERENCES users(id)
);
```

---

## order_items

(що саме купили)

```sql
CREATE TABLE order_items (
    id INT AUTO_INCREMENT PRIMARY KEY,
    order_id INT,
    product_id INT,
    quantity INT,

    FOREIGN KEY (order_id) REFERENCES orders(id),
    FOREIGN KEY (product_id) REFERENCES products(id)
);
```

---

## payments

```sql
CREATE TABLE payments (
    id INT AUTO_INCREMENT PRIMARY KEY,
    order_id INT,
    amount DECIMAL(10,2),
    payment_method VARCHAR(50),
    payment_date DATETIME DEFAULT CURRENT_TIMESTAMP,

    FOREIGN KEY (order_id) REFERENCES orders(id)
);
```

---

# 3. Тестові дані

## Users

```sql
INSERT INTO users (name,email,city) VALUES
('Ivan','ivan@gmail.com','Kyiv'),
('Anna','anna@gmail.com','Lviv'),
('Oleh','oleh@gmail.com','Kyiv'),
('Petro','petro@gmail.com','Odessa'),
('Maria','maria@gmail.com','Lviv');
```

---

## Products

```sql
INSERT INTO products (name,category,price) VALUES
('Laptop','Electronics',1200),
('Phone','Electronics',800),
('Keyboard','Electronics',100),
('Chair','Furniture',200),
('Table','Furniture',400);
```

---

## Orders

```sql
INSERT INTO orders (user_id) VALUES
(1),
(1),
(2),
(3),
(4);
```

---

## Order Items

```sql
INSERT INTO order_items (order_id,product_id,quantity) VALUES
(1,1,1),
(1,3,2),
(2,2,1),
(3,4,1),
(4,5,1),
(5,3,3);
```

---

## Payments

```sql
INSERT INTO payments (order_id,amount,payment_method) VALUES
(1,1400,'card'),
(2,800,'card'),
(3,200,'cash'),
(4,400,'card'),
(5,300,'card');
```

---

# 4. 5 реальних JOIN задач

---

# JOIN 1

## Показати всі замовлення з ім'ям користувача

```sql
SELECT
orders.id AS order_id,
users.name,
orders.order_date
FROM orders
JOIN users
ON orders.user_id = users.id;
```

---

# JOIN 2

## Показати що саме купили

```sql
SELECT
users.name,
products.name AS product,
order_items.quantity
FROM order_items
JOIN orders ON order_items.order_id = orders.id
JOIN users ON orders.user_id = users.id
JOIN products ON order_items.product_id = products.id;
```

---

# JOIN 3

## Загальна сума замовлення

```sql
SELECT
orders.id,
SUM(products.price * order_items.quantity) AS total
FROM order_items
JOIN products ON order_items.product_id = products.id
JOIN orders ON order_items.order_id = orders.id
GROUP BY orders.id;
```

---

# JOIN 4

## Скільки замовлень зробив кожен користувач

```sql
SELECT
users.name,
COUNT(orders.id) AS total_orders
FROM users
LEFT JOIN orders
ON users.id = orders.user_id
GROUP BY users.name;
```

---

# JOIN 5

## Сума покупок кожного користувача

```sql
SELECT
users.name,
SUM(payments.amount) AS total_spent
FROM users
JOIN orders ON users.id = orders.user_id
JOIN payments ON orders.id = payments.order_id
GROUP BY users.name;
```

---

# 5 Stored Procedures (5 прикладів)

---

# Procedure 1

## Отримати всі замовлення користувача

```sql
DELIMITER //

CREATE PROCEDURE get_user_orders(IN uid INT)
BEGIN

SELECT *
FROM orders
WHERE user_id = uid;

END //

DELIMITER ;
```

Запуск

```sql
CALL get_user_orders(1);
```

---

# Procedure 2

## Порахувати суму покупок користувача

```sql
DELIMITER //

CREATE PROCEDURE get_user_total(IN uid INT)
BEGIN

SELECT
users.name,
SUM(payments.amount) AS total
FROM users
JOIN orders ON users.id = orders.user_id
JOIN payments ON orders.id = payments.order_id
WHERE users.id = uid
GROUP BY users.name;

END //

DELIMITER ;
```

---

# Procedure 3

## Показати всі продукти у категорії

```sql
DELIMITER //

CREATE PROCEDURE get_products_by_category(IN cat VARCHAR(50))
BEGIN

SELECT *
FROM products
WHERE category = cat;

END //

DELIMITER ;
```

---

# Procedure 4

## Створити нове замовлення

```sql
DELIMITER //

CREATE PROCEDURE create_order(IN uid INT)
BEGIN

INSERT INTO orders(user_id)
VALUES(uid);

END //

DELIMITER ;
```

---

# Procedure 5

## Топ найдорожчих продуктів

```sql
DELIMITER //

CREATE PROCEDURE top_expensive_products()
BEGIN

SELECT *
FROM products
ORDER BY price DESC
LIMIT 3;

END //

DELIMITER ;
```

---

# 6. 20 SQL задач як на співбесіді Data Engineer

### Легкі

1️⃣ Показати всіх користувачів з міста `Kyiv`

```
SELECT * FROM users WHERE city='Kyiv';
```

---

2️⃣ Порахувати кількість користувачів

---

3️⃣ Знайти найдорожчий продукт

---

4️⃣ Показати всі замовлення

---

5️⃣ Порахувати кількість замовлень

---

### Середні

6️⃣ Порахувати кількість замовлень кожного користувача

---

7️⃣ Порахувати суму всіх платежів

---

8️⃣ Показати всі продукти які купив Ivan

---

9️⃣ Знайти середню ціну продукту

---

10️⃣ Показати топ 3 найдорожчі продукти

---

### JOIN задачі

11️⃣ Показати

```
user | order_id
```

---

12️⃣ Показати

```
user | product
```

---

13️⃣ Показати

```
order_id | total_price
```

---

14️⃣ Показати

```
user | total_spent
```

---

15️⃣ Показати

```
category | total_sales
```

---

### Складні

16️⃣ Знайти користувачів які зробили більше 1 замовлення

---

17️⃣ Знайти продукти які купували більше 2 разів

---

18️⃣ Знайти користувача який витратив найбільше

---

19️⃣ Показати міста з найбільшою кількістю користувачів

---

20️⃣ Показати день з найбільшою кількістю замовлень

